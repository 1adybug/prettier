# Rslib project

## Setup

Install the dependencies:

```bash
pnpm install
```

## Get started

Build the library:

```bash
nub run build
```

Build the library in watch mode:

```bash
nub run dev
```
