{
    "name": "@1adybug/tooling-monorepo",
    "version": "1.0.0",
    "description": "Shared ESLint and Prettier tooling for @1adybug",
    "keywords": [],
    "author": "",
    "license": "MIT",
    "type": "module",
    "private": true,
    "packageManager": "pnpm@11.10.0",
    "scripts": {
        "dev": "nub run --recursive --filter \"@1adybug/*\" --parallel dev",
        "r": "pnpm i && pnpm publish -r",
        "cs": "nub exec changeset && nub exec changeset version",
        "format": "nub exec prettier --write .",
        "sync": "nub scripts/syncPackage.mjs",
        "postr": "nub run sync",
        "build": "nub run --recursive --filter \"@1adybug/*\" build",
        "fg": "nub run format && git add . && git commit -m \"✨feature: format\"",
        "prelint": "nub run --filter \"@1adybug/eslint\" build",
        "lint": "nub exec eslint .",
        "prepare": "husky",
        "check": "nub run --recursive --filter \"@1adybug/*\" check",
        "test": "nub run --recursive --filter \"@1adybug/*\" --if-present test"
    },
    "devDependencies": {
        "@1adybug/eslint": "workspace:*",
        "@1adybug/prettier": "workspace:*",
        "@changesets/cli": "^2.29.8",
        "@types/node": "catalog:",
        "eslint": "^9.39.3",
        "husky": "^9.1.7",
        "lint-staged": "^17.0.7",
        "prettier": "catalog:",
        "typescript": "^6.0.3"
    },
    "lint-staged": {
        "**/*": "prettier --write --ignore-unknown"
    },
    "specifier": {},
    "workspaces": {
        "packages": [
            "packages/*"
        ],
        "catalog": {
            "@types/node": "^24.10.14",
            "prettier": "^3.8.1"
        }
    }
}
